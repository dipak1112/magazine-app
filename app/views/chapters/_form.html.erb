<%= form_for [@book, @chapter] do |f| %>
  <% if @chapter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chapter.errors.count, "error") %> prohibited this chapter from being saved:</h2>

      <ul>
      <% @chapter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :chapter_no, "Chapter Number" %><br />
    <%= f.number_field :chapter_no %>
  </div>

  <div class="field">
    <%= f.label :name, "Chapter Name" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Submit", :class => "btn btn-custom" %>
  </div>
<% end %>
